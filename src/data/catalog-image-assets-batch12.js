// Batch 3 HABA candidates were rolled back after duplicate-ref preflight found
// pre-existing, non-identical Catalog entries already using both official refs.
// Keep this batch intentionally empty so the import remains a durable audit point.
export const BATCH12_OFFICIAL_IMAGE_ROWS = Object.freeze([
]);
